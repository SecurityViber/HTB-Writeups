

python3 -c 'import pty;pty.spawn("bash")'

grep -r "password" . 2>/dev/null


grep -rEi "password|passwd|pwd|secret|token|api[_-]?key" . 2>/dev/null


grep -rEi "password|passwd|pwd|secret|token|api[_-]?key" . 2>/dev/null



find / -type d -readable \
  ! -path "/usr/lib/*" \
  ! -path "/usr/share/*" \
  ! -path "/proc/*" \
  ! -path "/sys/*" \
  ! -path "/snap/*" \
  2>/dev/null


  BUNDLE_HTTPS://RUBYGEMS__ORG/: "henry:Q3c1AqGHtoI0aXAYFH"


Q3c1AqGHtoI0aXAYFH