# Challenge: Alien Cradle (Forensics)

## Solution

1. Open the file — everything is on one line. Reformat by replacing each `;` with `;\n`.
2. The flag is split across several substrings concatenated with `' + '`. Replace all `' + '` with an empty string.
3. Flag: `HTB{p0w3rsh3ll_Cr4dl3s_c4n_g3t_th3_j0b_d0n3}`
